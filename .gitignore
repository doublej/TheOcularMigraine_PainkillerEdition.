node_modules/
dist/
.svelte-kit/
# The generated Capacitor projects are not tracked, but the hand-written native sources
# inside android/ are — they cannot be regenerated by `cap add android`. Those files are
# already tracked, so these rules no longer hide them; a NEW one needs `git add -f`.
android/
ios/
*.local
.DS_Store
.session-search*/
screenshots/raw/
screenshots/test-results/
